'use strict';

const titre = 'Pas de vacances à Noël';
const par = 'Lorem Ipsum etc';

const html = `
<article>
    <h1>${titre}</h1>
    <p>${par}</p>
</article>`;

console.log(html);

document.write(html);

